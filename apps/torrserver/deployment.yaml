apiVersion: apps/v1
kind: Deployment
metadata:
  name: torrserver
  labels:
    app: torrserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: torrserver
  template:
    metadata:
      labels:
        app: torrserver
    spec:
      containers:
        - name: torrserver
          image: yourok/torrserver:latest
          ports:
            - containerPort: 8090